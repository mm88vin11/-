# Secrets — never commit real API keys (this repository is public)
.env
.env.local
.mcp.local.json
.claude/settings.local.json

# Node
node_modules/

# The build configured with a live bot token — set-lead.py writes it, and it
# is the file you publish. It must never reach this public repository.
site/baza.local.html

# Python
__pycache__/
